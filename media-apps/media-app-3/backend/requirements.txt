fastapi[standard]==0.115.0
sqlalchemy==2.0.35
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
aiofiles==24.1.0
pillow==10.4.0
exifread==3.0.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
piexif==1.1.3

insightface==0.7.3
onnxruntime==1.20.1

# BLIP-2 dependencies
# Note: torch==2.4.0 (spec) has no Python 3.13 wheel; torch==2.10.0 (latest available) installed instead.
# Note: transformers==4.44.2 (spec) requires tokenizers<0.21 which fails to build on Python 3.13;
#       transformers==4.57.6 (latest 4.x) used instead, which requires tokenizers>=0.21 (pre-built wheel).
transformers==4.57.6
torch==2.10.0
accelerate==0.34.2

# Clustering dependencies
# Note: hdbscan==0.8.38 (spec) not available for Python 3.13; 0.8.41 used instead.
# Note: faiss-cpu==1.8.0 (spec) not available for Python 3.13; 1.13.2 used instead.
# Note: scikit-learn==1.5.2 (spec) already installed as insightface dep at 1.8.0; retained to avoid downgrade.
hdbscan==0.8.41
faiss-cpu==1.13.2
scikit-learn==1.8.0
